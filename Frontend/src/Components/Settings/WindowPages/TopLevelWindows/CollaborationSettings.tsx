import React from 'react';

type Props = {};

const CollaborationSettings = (props: Props) => {
	return (
		<div>
			<h2>Collaboration Settings</h2>
		</div>
	);
};

export default CollaborationSettings;
